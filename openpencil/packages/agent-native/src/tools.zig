// src/tools.zig
pub const registry = @import("tools/registry.zig");
pub const ToolRegistry = registry.ToolRegistry;
pub const delegate = @import("tools/delegate.zig");
pub const DelegateArgs = delegate.DelegateArgs;
