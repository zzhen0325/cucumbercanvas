/**
 * Layout reorder insertion indicator overlay.
 *
 * Previously rendered via Fabric.js `after:render` hook.
 * Now a no-op — layout indicators are rendered by the Skia overlay system.
 */
export function useLayoutIndicator() {
  // No-op: layout indicators are handled by the SkiaEngine overlay renderer.
}
